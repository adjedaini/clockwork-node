# Clockwork — optional env config (all keys prefixed with CLOCKWORK_)
# Copy to .env and adjust. In-code config overrides env.

# Enable/disable Clockwork (default: auto from NODE_ENV)
# CLOCKWORK_ENABLED=true

# Base path for API and dashboard (default: /__clockwork)
# CLOCKWORK_PATH=/__clockwork

# Intercept console.log/info/warn/error per request (default: true when NODE_ENV !== production)
# CLOCKWORK_INTERCEPT_NATIVE_LOGS=true

# Capture request/response bodies (default: true in dev)
# CLOCKWORK_CAPTURE_REQUEST_BODY=true
# CLOCKWORK_CAPTURE_RESPONSE_BODY=true

# Limits
# CLOCKWORK_MAX_BODY_SIZE=10000
# CLOCKWORK_MAX_BODY_DEPTH=10
# CLOCKWORK_MAX_REQUESTS=100

# Path prefixes to ignore, comma-separated
# CLOCKWORK_IGNORE_PREFIXES=/admin,/health,/internal

# Keys to redact in headers/body (comma-separated). Replaces default list when set. Set empty to disable redaction.
# CLOCKWORK_REDACT_KEYS=authorization,password,secret,token,api_key

# Dashboard title
# CLOCKWORK_TITLE=Clockwork

# Protect dashboard and API with HTTP Basic Auth (when both are set)
# CLOCKWORK_DASHBOARD_USERNAME=admin
# CLOCKWORK_DASHBOARD_PASSWORD=your-secret-password

# Health checks as JSON array (optional)
# CLOCKWORK_HEALTH_CHECKS=[{"protocol":"http","host":"localhost","path":"/health","port":3000,"name":"App"}]
